name: Run automated job

on:
  push:
    branches:
      - "main"
    
jobs:
  tests:
    runs-on: ubuntu-lastest
    
    steps:
      - name: build
        uses: dotnet build
      - name: test
        uses: dotnet test
    
