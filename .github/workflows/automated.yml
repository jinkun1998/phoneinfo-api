name: Run automated job

on:
  push:
    branches:
      - "main"
    
jobs:
  tests:
    runs-on: ubuntu-lastest
    
    steps:
      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v3.0.3
        with:
          dotnet-version: 5.0
      - name: build
        run: dotnet build
      - name: test
        run: dotnet test
    
